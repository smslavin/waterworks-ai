[project]
name = "mqtt-influx-bridge"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "paho-mqtt>=2.0",
    "influxdb-client>=1.40",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
