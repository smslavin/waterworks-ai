[project]
name = "simulator"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "asyncua>=1.1.0",
    "paho-mqtt>=2.0.0",
    "aiohttp>=3.9.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
    "fieldworks-core>=1.0.1",
]

[tool.uv]
package = false
